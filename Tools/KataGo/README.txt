KataGo 本机目录（引擎与权重不进 Git）

需要：
  katago.exe            OpenCL / GPU 版
  katago-eigen.exe      CPU AVX2 备用
  default_model.bin.gz  神经网络权重
  gtp.cfg               GTP 配置（本仓库提供）

人机对弈选「KataGo」即可。首次 OpenCL 可能自动调优，稍等即可。
